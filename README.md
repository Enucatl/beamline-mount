# SLS scripts for non-SL people

Easy installation:

```
    git clone https://github.com/Enucatl/beamline-mount.git
    cd beamline-mount
    python setup.py develop
    pyenv rehash
    blmount --help
```
